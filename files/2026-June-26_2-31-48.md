Stateless server A stateless server treats every request as a new, independent event. When a request arrives, the server should have everything it needs to process it: authentication information requested resource input data relevant identifiers any needed context After sending the response, that particular server does not need to remember the client. 