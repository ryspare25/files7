A stateless API can still use a database: Request → API server → Database → Response It means the API server itself does not keep a client’s temporary conversational/session context in its own memory between requests. For example, the database may store: customer accounts orders products payments But Server A does not have to remember that it handled Jade’s request five minutes ago.